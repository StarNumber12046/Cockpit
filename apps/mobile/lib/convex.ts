/**
 * Convex API for the mobile app (backend package generated client).
 * After linking a deployment, run `npx convex dev` in packages/backend
 * so `_generated` matches the live schema.
 */
export { api } from "@cockpit/backend/api";
